## 2026-08-14 - [Replace Deprecated fitz Import with pymupdf] **The Problem:** The PyMuPDF `fitz` module is deprecated and issues runtime warnings, requiring migration to `pymupdf`. **The Execution:** Updated the `fitz` import in `src/pdf_to_txt.py` to `import pymupdf as fitz` to resolve the warning and updated the verification script (`scripts/verify_environment.py`) to check for `pymupdf` instead of `fitz`. Documentation in `docs/` was also updated accordingly. **The Impact:** The deprecation warnings are removed, and the environment script now properly checks for the modern library name.
