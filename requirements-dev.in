# requirements-dev.in -- unpinned source declarations for pip-tools (dev only)
# Compile with: pip-compile requirements-dev.in --generate-hashes -o requirements-dev.txt

# Linting & formatting
ruff>=0.4.0
mypy>=1.10

# Security scanning
bandit[sarif]>=1.7.9

# Dependency auditing
pip-audit>=2.7.0

# Testing
pytest>=8.0
pytest-cov>=5.0
pytest-timeout>=2.3

# Pre-commit hooks
pre-commit>=3.7.0

# Windows release packaging
pyinstaller>=6.22.0
