# requirements.in -- unpinned source declarations for pip-tools
# Compile with: pip-compile requirements.in --generate-hashes -o requirements.txt
# Upgrade all:  pip-compile --upgrade requirements.in --generate-hashes -o requirements.txt

# Core PDF / OCR
pytesseract>=0.3.10
Pillow>=10.0
PyMuPDF>=1.23
numpy>=1.26

# AI / LLM
ollama>=0.1.7
anthropic>=0.31.1
openai

# GUI / Web
pywebview>=5.0
Flask>=3.0
waitress>=3.0
PyJWT>=2.8

# Calendar
icalendar>=5.0

# Face detection
mediapipe>=0.10.9
opencv-python>=4.9

# Voice / Speech
pyttsx3>=2.90
SpeechRecognition>=3.10

# Google Calendar integration
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.1

# Cryptography & Security
cryptography>=42.0
pyopenssl>=24.0
pywin32>=306
sentry-sdk>=2.0
python-dotenv>=1.0.0

# PDF export
fpdf2>=2.7

# Speech recognition (offline)
vosk>=0.3
